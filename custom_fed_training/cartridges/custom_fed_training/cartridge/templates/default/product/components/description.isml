<isif condition="${product.shortDescription}">
<iscomment> col-md-8 col-lg-9  </iscomment>
    <div class="value content" id="collapsible-description-${loopState && loopState.count ? loopState.count : '1'}">
        <isprint value="${product.shortDescription}" encoding="off" />
    </div>
</isif>

